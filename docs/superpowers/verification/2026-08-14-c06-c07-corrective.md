# C06/C07 Corrective Verification

**Branch:** `codex/c06-c07-corrective`
**Base:** `origin/main` at `b906bdc`
**Commits:** `8513141`, `f5c0252`

## Scope Verified

- C06 request context is provider-neutral and carries affected sections, resource/revision provenance, scoped overrides and versioned design/spec/build profiles.
- `waiting_for_input` is a non-terminal result with a stable `PendingDecision`; provider events cannot express Promote, Publish or Resource Review decisions.
- C07 persists pending decisions and decision-aware events. A refresh or reconnect reads the same run and pending decision.
- Continuation resolves the decision on the same run, appends `build.continued` at the next sequence, and identical retries are idempotent.
- `waiting_for_input` survives orphan recovery. Cancellation moves through `cancelling`; terminal confirmation closes unresolved decisions.
- JSON replay and SSE `Last-Event-ID` emit only events after the requested sequence.
- The existing BuildService has a narrow waiting-state guard: a repeated execute request returns the persisted pending decision and does not start a second provider run.

## Verification Commands

```text
/Users/zhaozhuo/workspace/explore/ai-cowork-game/.worktrees/platform-backend-foundation/backend/.venv/bin/pytest backend/tests/test_c06_game_agent_contract.py backend/tests/contract_suites/test_game_agent_contract_suite.py -q
# passed as part of the full backend run

/Users/zhaozhuo/workspace/explore/ai-cowork-game/.worktrees/platform-backend-foundation/backend/.venv/bin/pytest backend/tests/test_c07_run_events.py backend/tests/test_c07_run_api.py -q
# 19 passed, 1 warning

/Users/zhaozhuo/workspace/explore/ai-cowork-game/.worktrees/platform-backend-foundation/backend/.venv/bin/pytest backend/tests -q
# 114 passed, 1 warning

DATABASE_URL="sqlite:////private/tmp/c06-c07-repeatability.sqlite" \
  /Users/zhaozhuo/workspace/explore/ai-cowork-game/.worktrees/platform-backend-foundation/backend/.venv/bin/alembic upgrade head
# executed twice against the same database without error

npm run build
# vue-tsc -b and vite build passed

git diff --check
# no whitespace errors
```

## Scope Guard

BuildService changed only for the waiting-state guard needed to prevent duplicate provider starts; no OpenGame adapter, Candidate/Promote/Publish, Resource Review or Workspace UI files were changed. C06/C07 define and persist the provider-neutral run contract; they do not make platform Human Gate decisions.

## Baseline Note

This corrective branch starts from the merged C05 core at `origin/main` (`b906bdc`). The later local C05 frontend follow-up commits `cee66b7`, `e8bc261` and `b5b652d` are not present on `origin/main`; that unrelated baseline gap is intentionally not folded into this corrective change.
