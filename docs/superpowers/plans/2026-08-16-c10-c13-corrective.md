# C10/C13 Corrective Implementation Plan

> **For agentic workers:** Execute this plan task-by-task with a test-first loop.

**Goal:** Restore C10 prompt/result/concurrency correctness and C13 symlink/artifact validation while preserving existing provider-neutral boundaries.

**Architecture:** Keep OpenGame parsing and prompt construction private to the adapter package. Keep workspace policy in `WorkspaceManager`; `BuildService` continues to receive only `GameAgent` results. Every behavior change gets a focused regression test before production code.

**Tech Stack:** Python 3.11, FastAPI backend contracts, pytest, SQLAlchemy fixtures, Vue 3/Vite verification.

## Global Constraints

- Do not add Pinia, Vue Router, backend APIs, runtime dependencies, or provider-specific fields.
- Do not wire OpenGame into application defaults in this corrective change.
- Preserve the C06 `GameAgent` protocol and C09 executor boundary.

### Task 1: Regression tests

**Files:**
- Modify: `backend/tests/test_c10_game_agent_contract.py`
- Modify: `backend/tests/test_c10_build_result_from_fixtures.py`
- Modify: `backend/tests/test_c13_adapter_workspace.py`
- Modify: `backend/tests/test_c13_workspace.py`

- [ ] Add failing tests for prompt contents, malformed stream failure, complete artifact manifests, late cancel precedence, and active-run rejection.
- [ ] Run the focused tests and confirm each fails for the reported root cause.

### Task 2: Adapter correctness

**Files:**
- Modify: `backend/app/agents/opengame_adapter.py`
- Modify: `backend/app/agents/opengame_stream_parser.py`

- [ ] Add deterministic prompt serialization and pass it to the CLI.
- [ ] Preserve natural completion over late cancel and reject concurrent active runs.
- [ ] Treat parser errors and cancelled provider terminal events as standard results.
- [ ] Run the focused C10 tests.

### Task 3: Workspace validation

**Files:**
- Modify: `backend/app/agents/workspace.py`

- [ ] Check symlink components before resolving the candidate path.
- [ ] Scan bounded regular-file manifests with SHA-256 and a stable preview entry.
- [ ] Run the focused C13 tests.

### Task 4: Full verification

**Files:**
- No additional production files.

- [ ] Run the full backend suite and record failures/skips honestly.
- [ ] Run `npm --prefix frontend run build`.
- [ ] Run `git diff --check` and review the final diff.
